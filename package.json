{
  "private": true,
  "workspaces": {
    "packages": [
      "backend",
      "frontend",
      "shared"
    ],
    "nohoist": [
      "**/@nestjs/",
      "**/@nestjs/**"
    ]
  },
  "scripts": {
    "lint": "lerna run lint --no-bail",
    "type-check": "lerna run type-check --no-bail",
    "prepare": "husky install"
  },
  "name": "twoje-osk",
  "devDependencies": {
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  }
}
