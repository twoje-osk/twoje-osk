import { OptionsObject } from 'notistack';

export const SNACKBAR_DEFAULT_OPTIONS: OptionsObject = {
  autoHideDuration: 2000,
  anchorOrigin: {
    vertical: 'top',
    horizontal: 'center',
  },
};
